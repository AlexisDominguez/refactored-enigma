import { FC } from "react";
import { StyledTextArea } from "./TextAreaStyles";

// Text Area Interface:
// (property) JSX.IntrinsicElements.textarea: React.DetailedHTMLProps<React.TextareaHTMLAttributes<HTMLTextAreaElement>, HTMLTextAreaElement>

const TextArea: FC = () => {
  return <StyledTextArea />;
};

export default TextArea;
